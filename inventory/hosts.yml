all:
  children:
    proxmox_vms:
      hosts:
        nas-nijmegen:
          ansible_host: nas-nijmegen.hake-cod.ts.net
        services:
          ansible_host: services.hake-cod.ts.net
        sandbox:
          ansible_host: sandbox.hake-cod.ts.net
          ansible_ignore_unreachable: yes
